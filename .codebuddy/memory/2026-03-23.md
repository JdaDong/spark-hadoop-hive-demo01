# 2026-03-23 工作日志

## 新增高级实战代码模块

在已有的 Flink/Kafka/Iceberg/ClickHouse 基础示例之上，进一步补充了以下深度实战代码：

### 新增 Java 文件
1. `FlinkTableApiJavaApp.java` - Flink Table API & SQL 高级示例（DDL、窗口函数、Temporal Join、MATCH_RECOGNIZE、Mini-Batch优化、UDF）
2. `KafkaStreamsJavaApp.java` - Kafka Streams 流处理（KStream/KTable、窗口、连接、实时词频统计/用户行为分析/欺诈检测）
3. `SparkStructuredStreamingJavaApp.java` - Spark Structured Streaming（Kafka集成、窗口、水位线、有状态处理、ForeachBatch多路输出）
4. `RealtimeDataWarehouseApp.java` - 实时数仓实战（ODS→DWD→DWS→ADS四层架构，Flink CDC + Kafka + Flink SQL + ClickHouse）

### 新增基础设施
5. `docker-compose.yml` - 一键部署环境（Zookeeper + Kafka + Flink + ClickHouse + MySQL + Kafka UI + Grafana）
6. `scripts/mysql-init/init.sql` - MySQL 初始化脚本（电商数据库，含测试数据，CDC用户授权）

### 更新
- `pom.xml` 添加 spark-streaming 和 spark-sql-kafka 依赖
- `README.md` 更新项目结构、功能特性、运行命令

## 新增 Airflow 调度 & Atlas 数据治理模块

### Airflow DAG 文件 (airflow/dags/)
1. `bigdata_etl_pipeline_dag.py` - 大数据 ETL 全链路调度
   - 3个 DAG: 主ETL Pipeline + 数据回刷 + 数据质量监控
   - 6阶段任务编排: 采集(Sqoop) → ODS → 质量检查 → DWD → DWS → ADS(ClickHouse)
   - 自定义 Operator: ClickHouseOperator、HivePartitionSensor
   - 数据质量检查: 行数/空值率/唯一性/范围/跨表一致性 5种检查类型
   - SLA 监控、失败告警回调、指数退避重试

2. `realtime_pipeline_dag.py` - 实时流处理管理
   - 3个 DAG: Flink 作业健康检查(5min) + Savepoint 备份(2h) + Kafka Lag 监控(10min)
   - Flink REST API 完整封装: 集群管理/作业提交/Savepoint/Checkpoint
   - 自动恢复失败的 Flink 作业(从 Savepoint 恢复)
   - Kafka Consumer Lag 告警阈值: WARNING 10万 / CRITICAL 100万

3. `atlas_governance_dag.py` - Atlas 数据治理自动化
   - 4个 DAG: 血缘自动注册 + 治理巡检(每日) + 变更监控(30min) + 资产盘点(每周)
   - 治理评分体系: 元数据完整性+分类覆盖率+血缘完整性+PII合规 = 综合评分
   - 关键血缘链路完整性校验
   - Atlas REST API 客户端封装

### Atlas Java 示例
4. `AtlasGovernanceApp.java` - Atlas 数据治理完整实战
   - 10大模块: 类型系统/实体管理/表级血缘/列级血缘/分类标签/术语表/搜索查询/实时数仓血缘/质量元数据/Hook集成
   - 自定义类型: DataProduct、ETLProcess、DataQualityRule、DataGovernance
   - 完整分类体系: PII/安全等级(4级)/质量认证/GDPR/保留策略
   - 业务术语表: GMV/DAU/转化率/客单价/新用户 口径定义

### 基础设施更新
- `docker-compose.yml` 新增: Airflow(WebServer+Scheduler+PostgreSQL+Init) + Atlas + Solr
- `pom.xml` 添加 atlas-client-v2、atlas-common 依赖
- `README.md` 更新项目结构、功能特性(Airflow/Atlas/集成)、运行命令

## 新增 Prometheus 监控 & 数据湖 & MLflow 机器学习模块

### 新增 Java 文件
1. `PrometheusMonitoringApp.java` - Prometheus + Grafana 大数据全栈监控
   - 7 大指标域: ETL/Kafka/Flink/HDFS/Spark/ClickHouse/端到端延迟
   - 40+ 自定义 Prometheus 指标 (Counter/Gauge/Histogram/Summary)
   - 3 个采集器: FlinkCollector/KafkaCollector/HDFSCollector
   - 15 条告警规则引擎 + SLA 合规检查 + 自动扩缩容评估

2. `DataLakeApp.java` - 数据湖 Delta Lake + Apache Hudi
   - Delta Lake: MERGE INTO/Time Travel/Schema Evolution/CDF/Z-ORDER/VACUUM
   - Apache Hudi: COW表/MOR表/Upsert/增量查询/Hive同步
   - Medallion 分层: Bronze(原始)→Silver(清洗去重)→Gold(业务聚合)

3. `MLPlatformApp.java` - MLflow + Spark MLlib 机器学习平台
   - 用户流失预测: GBT/RF/LR 三模型对比 + CrossValidator 调优
   - 销售额预测: GBT回归/线性回归 + RMSE/MAE/R²评估
   - 用户分群: K-Means + RFM + 肘部法则
   - 商品推荐: ALS 协同过滤 + Top-N
   - MLflow 实验追踪/模型注册/A/B测试/Feature Store

### 新增配置文件
4. `scripts/monitoring/prometheus.yml` - Prometheus 采集配置 (8 个 Job)
5. `scripts/monitoring/alert_rules.yml` - 告警规则 (5 组 15 条)

### 基础设施更新
- `docker-compose.yml` 新增: Prometheus + Alertmanager + Node Exporter + MLflow
- `pom.xml` 添加: prometheus-client / delta-core / hudi-spark / spark-mllib
- `README.md` 更新项目结构、功能特性、运行命令

## 新增 数据安全 & 实时 OLAP & 图计算 模块

### 新增 Java 文件
1. `DataSecurityApp.java` - 大数据安全体系 (Ranger + Kerberos)
   - Kerberos 认证管理: KDC/TGT/Keytab/委托令牌/TGT续期
   - Ranger 策略引擎: 12+ 条策略 (HDFS/Hive/HBase/Kafka)
   - RBAC/ABAC/行级过滤/列级脱敏/标签策略/策略缓存
   - 数据脱敏: 手机/身份证/姓名/邮箱/银行卡/IP/SHA-256 (8种规则)
   - 数据加密: AES-256-GCM 列级加密 + 密钥管理
   - 数据分级分类: 自动发现敏感字段 (绝密/机密/内部/公开)
   - 安全审计: 操作日志/异常检测/合规报告/GDPR

2. `RealtimeOlapApp.java` - 实时 OLAP 引擎 (Doris + StarRocks)
   - Doris 三种表模型: Duplicate(明细)/Aggregate(聚合)/Unique(去重)
   - BITMAP 精确去重 + HLL 近似去重 + 动态分区 + 冷热分离
   - 同步/异步物化视图 + 透明查询改写
   - 4种数据导入: Stream Load/Routine Load/Broker Load/INSERT SELECT
   - StarRocks Primary Key 实时 Upsert + Colocate Join
   - 湖仓一体: Hive/Iceberg/Hudi/Delta Lake External Catalog (4个)
   - 6 大分析场景: 实时大盘/漏斗/留存/TopN/同环比/路径分析
   - 性能优化: 分区裁剪/Bucket/索引/Pipeline/Runtime Filter

3. `GraphComputingApp.java` - 图计算 (Neo4j + Spark GraphX)
   - Neo4j 图建模: User/Product/Category 属性图 + 约束/索引
   - Neo4j Cypher: 社交推荐/协同过滤/社区发现/PageRank/最短路径
   - Spark GraphX: 从 RDD 构建图/入度出度/PageRank/ConnectedComponents/TriangleCount
   - Pregel 消息传递: 影响力传播模拟
   - 反欺诈: 设备关联/资金环路/风险传播/团伙识别 (4个场景)
   - 知识图谱: 商品关系/兴趣推理/供应链追溯

### 基础设施更新
- `docker-compose.yml` 新增: Doris FE+BE + StarRocks FE+BE + Neo4j (含 GDS 插件)
- `pom.xml` 添加: spark-graphx / neo4j-java-driver / mysql-connector-java
- `README.md` 更新项目结构、功能特性、运行命令

## 新增 数据编排 & Serverless 计算 & 数据可观测性 模块

### 新增 Java 文件
1. `DataOrchestrationApp.java` - 数据编排平台 (dbt + Airbyte)
   - dbt 项目结构: staging/intermediate/marts 三层架构
   - dbt 增量模型 / 快照 SCD Type 2 / 测试体系 / Jinja2 宏 / Packages
   - Airbyte: MySQL CDC / REST API / S3 → Doris / PostgreSQL
   - Airflow + dbt Cosmos 集成 / 端到端 ELT Pipeline
   - 数据质量门禁 + Schema Contract + CI/CD (GitHub Actions Slim CI)

2. `FlinkServerlessApp.java` - Serverless 计算 (Flink on K8s)
   - Flink K8s Operator: Application/Session 模式 + Savepoint 管理
   - Pod Template / ResourceQuota / RBAC
   - Flink Autoscaler + KEDA (Kafka Lag) + HPA + Spot Instance
   - Docker 多阶段构建 + Helm Chart + ArgoCD GitOps + Canary 发布
   - 多租户隔离 + 成本核算

3. `DataObservabilityApp.java` - 数据可观测性 (OpenLineage + Marquez)
   - OpenLineage 事件模型 + Facets (Schema/ColumnLineage/DataQuality)
   - Marquez API + Web UI (血缘/版本/作业历史/搜索)
   - Spark/Flink/Airflow/dbt OpenLineage 集成
   - 跨系统端到端血缘 + 字段级血缘
   - 数据质量四维度 + Pipeline 健康度 + 漂移检测
   - 影响分析 + 根因分析 + SLA 告警 + 合规追踪

### 基础设施更新
- `docker-compose.yml` 新增: Airbyte Server+DB + Marquez API+Web+DB
- `pom.xml` 添加: openlineage-spark / openlineage-java
- `README.md` 更新项目结构、功能特性、运行命令

## 新增 数据契约 & 向量数据库 & 特征工程 & DataOps & CDC 全链路 模块

### 新增 Java 文件
1. `DataContractApp.java` - 数据契约 & Data Mesh & Data API
   - Data Contract Specification v0.9.3 + Schema 演进 + CLI 工具链
   - Data Mesh 四大原则 + Data Product YAML + 联邦治理
   - Data API (GraphQL + REST OpenAPI 3.0 + gRPC)
   - 契约测试 CI/CD + OPA 合规策略 + Data Product Canvas

2. `VectorDatabaseApp.java` - 向量数据库 & LLM RAG Pipeline
   - Milvus 2.x 架构 + Java SDK + Embedding 模型选型
   - 企业级 RAG 8步架构 (HyDE/RRF/Reranker/幻觉检测)
   - 混合搜索 + 向量索引策略 + 大数据AI融合架构

3. `FeatureStoreApp.java` - 实时特征工程 Feature Store (Feast)
   - Feature Store 四层架构 + Feast 配置 + FeatureView/Service
   - Flink SQL 实时特征 + 流批统一 + Online/Historical Serving
   - 特征监控 + 漂移检测 (PSI/KS Test)

4. `DataOpsApp.java` - DataOps 平台工程 & IaC
   - Terraform IaC + K8s 大数据平台 + ArgoCD GitOps
   - 自助数据平台 + 多租户 + CI/CD for Data + FinOps

5. `CDCPipelineApp.java` - CDC 全链路 (Debezium + Flink CDC 3.0)
   - Debezium 生产级配置 + Flink CDC 3.0 YAML Pipeline
   - 增量快照算法 + Schema Evolution + Exactly-Once
   - CDC 运维 + 最佳实践 & 踩坑指南

### 基础设施更新
- `docker-compose.yml` 新增: Schema Registry + Milvus全套 + Feast+Redis
- `pom.xml` 添加: milvus-sdk-java / flink-connector-mysql-cdc / flink-cdc-pipeline
- `README.md` 更新项目结构、功能特性、运行命令、参考文档
